services:
  - type: web
    name: plum-assignment-backend
    runtime: docker
    plan: free
    dockerfilePath: ./docker/backend.Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: plum-assignment-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        value: https://plum-assignment.vercel.app,https://plum-assignment-sand.vercel.app,http://localhost:3000
      - key: CORS_ORIGIN_REGEX
        value: https://.*\.vercel\.app
      - key: EASYOCR_DOWNLOAD_ENABLED
        value: "true"
      - key: OPENAI_MODEL
        value: gpt-4o

databases:
  - name: plum-assignment-db
    plan: free
    databaseName: plum
    user: plum
